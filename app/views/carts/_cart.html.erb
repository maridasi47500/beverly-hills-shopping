<div id="<%= dom_id cart %>">
  <h2>
    <%=t(".title")%>
  </h2>
  <table>
    <%cart.line_items.each do |item|%>
      <tr>
      <td>
        <%=item.product.quantity%>
      </td>
      <td>
        &times;
      </td>
      <td>

        <%=item.product.title%>
      </td>
      <td>

        <%=nummber_to_currency item.total_price%>
      </td>

      </tr>
    <%end%>
    <tfoot>
      <tr>
        <th>
          total:
        </th>
        <td>
        <%=nummber_to_currency cart.total_price%>

        </td>
      </tr>
    </tfoot>
  </table>
  <%=button_to t(".empty"),cart,method: :delete%>
  <%=button_to t(".checkout"),new_order_path,method: :get%>
</div>
